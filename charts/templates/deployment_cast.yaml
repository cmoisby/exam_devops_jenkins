apiVersion: apps/v1
kind: Deployment
metadata:
  name: cast-service
  labels:
    app: cast-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: cast-service
  template:
    metadata:
      labels:
        app: cast-service
    spec:
      containers:
        - name: cast-service
          image: {{ .Values.castService.image }}:{{ .Values.castService.tag }}
          command:
            - /bin/sh
            - -c
            - |
              echo "Waiting for DB..." &&
              sleep 30 &&  # Attendre 30 secondes avant de commencer
              uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
          ports:
            - containerPort: 8000
          env:
            - name: DATABASE_URI
              value: {{ .Values.castService.databaseUri }}
          volumeMounts:
            - name: cast-service-volume
              mountPath: /app
      volumes:
        - name: cast-service-volume
          hostPath:
            path: /home/ubuntu/exam_devops_jenkins/cast-service
            type: Directory
