apiVersion: v1
kind: Service
metadata:
  labels:
    app: movie-db 
  name: movie-db 
spec:
  ports:
  - port: 5432
    protocol: TCP
    targetPort: 5432
  selector:
    app: movie-db
  type: ClusterIP